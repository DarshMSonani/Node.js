set Is For Add Values When Add In Data Base

get Is Used For Add Values When Get The Data From DataBase

set(value) {
    this.setDataValue("name", value + "Data")
}

get() {
    return this.getDataValue("name) + "xyz";
}